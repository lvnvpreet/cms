/**
 * Layout Index File
 *
 * Purpose: Export file that makes layout components available to other parts of the application.
 *
 * Functionality:
 * - Exports all layout components for easy imports elsewhere.
 * - May provide composed layout patterns for common scenarios.
 * - Could include layout-related hooks or utilities.
 */

export { default as AppLayout } from './AppLayout';
export { default as Sidebar } from './Sidebar';
export { default as Navbar } from './Navbar';
export { default as ContentArea } from './ContentArea';
export { default as Footer } from './Footer';

// TODO: Export composed layout patterns if needed
// TODO: Export layout-related hooks or utilities if needed
